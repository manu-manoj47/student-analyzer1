version: "3.8"
services:
  backend:
    build: ./backend
    container_name: student-backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    container_name: student-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend